{{#each dbPostData as |post| }}
  <h2>
    {{post.title}}
  </h2>
  <p>Written by: User ID {{post.user_id}}</p>
  <p>
    {{format_date post.date_created}}
  </p>
  <p>
    {{post.content}}
  </p>
  <div class="post link">
    <a id="readMore" href="/post/{{id}}">Read More</a>
  </div>
{{/each}}

